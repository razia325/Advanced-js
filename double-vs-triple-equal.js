const first = 2;
const second = 3;
if (first === second) {
    console.log("condition is true");
} else {
    console.log("condition is false");
}
// (==)don't check data-type-- that sign convert type with other and try to make them same
// (===)check value and data-type both
// true compare with 1
// false compare with 0